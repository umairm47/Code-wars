//We need a function that can transform a number (integer) into a string.What ways of achieving this do you know?



/**
 * Parameters - number
 * Return - string
 * Examples
 *   123  --> "123"
     999  --> "999"
    -100 --> "-100" ;
 
 * Pseudocode
   I can use the built in method .toString() on the number to convert it to string and return it

 */
   function numberToString(num) {
    return num.toString();
  }